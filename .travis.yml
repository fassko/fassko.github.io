language: ruby
sudo: false
cache: bundler
rvm: 2.6.3

script:
  - bundle exec jekyll build

branches:
  except:
    - master
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: _site
  target-branch: master
  on:
    branch: develop
after_deploy:
  - ./clear_cache.sh
