name: Test inputs

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Define environment'
        default: 'staging'
        required: true
        type: string
        
jobs:
  printInputs:
    runs-on: ubuntu-latest
    steps:
      - name: Send greeting
        run: echo "${{ inputs.environment }}"
