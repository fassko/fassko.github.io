---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <ul class="blog-tags">
      {tags.map((tag) => (
        <li>
          <a href={`/tags/${encodeURIComponent(tag.toLowerCase())}`}>{tag}</a>
        </li>
      ))}
    </ul>
  )
}
